# AI 数据查询助手 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 在宏发金属交易市场管理系统中接入 DeepSeek 大模型，创建 Text-to-SQL Agent，支持后台管理和微信商户端多轮对话查询业务数据。

**架构：** 轻量自定义 Agent，直接调用 DeepSeek API（OpenAI 兼容格式），自定义实现 Agent 逻辑。用户提问 → 构建 Prompt（含 schema + 权限）→ DeepSeek 生成 SQL → 安全校验 → 执行 → 结果喂回 DeepSeek → 自然语言回答。对话历史存储在 SQL Server 新建的两张表中。

**技术栈：** Python 3.11 / Flask / pyodbc / DeepSeek API (openai 包) / Bootstrap 5 / jQuery

---

## 文件结构

| 操作 | 文件路径 | 职责 |
|------|---------|------|
| 创建 | `scripts/create_agent_tables.sql` | 建表 SQL 脚本 |
| 创建 | `app/services/agent_sql_validator.py` | SQL 安全校验 |
| 创建 | `app/services/agent_prompt_builder.py` | Prompt 构建（含 schema） |
| 创建 | `app/services/agent_service.py` | Agent 核心服务 |
| 创建 | `app/routes/agent.py` | Agent 路由 Blueprint |
| 修改 | `app/__init__.py` | 注册 agent_bp 蓝图 |
| 修改 | `config/base.py` | 添加 DeepSeek 配置项 |
| 创建 | `templates/agent/chat.html` | 后台 AI 助手页面 |
| 创建 | `templates/agent/wx_chat.html` | 微信端对话页面 |
| 修改 | `templates/admin_base.html` | 侧边栏添加 AI 助手菜单 |

---

### 任务 1：创建数据库表

**文件：**
- 创建：`scripts/create_agent_tables.sql`

- [ ] **步骤 1：编写建表 SQL**

```sql
-- AI Agent 对话会话表
IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'AgentConversation')
BEGIN
    CREATE TABLE AgentConversation (
        ConversationID INT IDENTITY(1,1) PRIMARY KEY,
        UserID INT NOT NULL,
        Title NVARCHAR(200) NOT NULL DEFAULT N'新对话',
        Source NVARCHAR(20) NOT NULL DEFAULT N'admin',
        CreateTime DATETIME NOT NULL DEFAULT GETDATE(),
        UpdateTime DATETIME NOT NULL DEFAULT GETDATE(),
        FOREIGN KEY (UserID) REFERENCES [User](UserID)
    );
END;

-- AI Agent 对话消息表
IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'AgentMessage')
BEGIN
    CREATE TABLE AgentMessage (
        MessageID INT IDENTITY(1,1) PRIMARY KEY,
        ConversationID INT NOT NULL,
        Role NVARCHAR(20) NOT NULL,
        Content NVARCHAR(MAX) NOT NULL,
        GeneratedSQL NVARCHAR(MAX) NULL,
        QueryResult NVARCHAR(MAX) NULL,
        CreateTime DATETIME NOT NULL DEFAULT GETDATE(),
        FOREIGN KEY (ConversationID) REFERENCES AgentConversation(ConversationID)
    );
END;

-- 索引
IF NOT EXISTS (SELECT * FROM sys.indexes WHERE name = 'IX_AgentConversation_UserID')
    CREATE INDEX IX_AgentConversation_UserID ON AgentConversation(UserID);

IF NOT EXISTS (SELECT * FROM sys.indexes WHERE name = 'IX_AgentMessage_ConversationID')
    CREATE INDEX IX_AgentMessage_ConversationID ON AgentMessage(ConversationID);
```

- [ ] **步骤 2：执行建表脚本**

运行：`python -c "from utils.database import execute_query; execute_query(open('scripts/create_agent_tables.sql', encoding='utf-8').read(), fetch_type='none'); print('Tables created')"`
预期：无报错，表创建成功

- [ ] **步骤 3：验证表已创建**

运行 SQL 确认两张表存在：
```sql
SELECT TABLE_NAME FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME IN ('AgentConversation', 'AgentMessage')
```
预期：返回两行

---

### 任务 2：添加配置项

**文件：**
- 修改：`config/base.py`

- [ ] **步骤 1：在 Config 类中添加 DeepSeek 配置**

在 `config/base.py` 的 `Config` 类中，`WX_TEMPLATE_BIND_RESULT` 之后添加：

```python
    # DeepSeek AI Agent 配置
    DEEPSEEK_API_KEY = os.environ.get('DEEPSEEK_API_KEY', '')
    DEEPSEEK_BASE_URL = os.environ.get('DEEPSEEK_BASE_URL', 'https://api.deepseek.com')
    DEEPSEEK_MODEL = os.environ.get('DEEPSEEK_MODEL', 'deepseek-chat')
    AGENT_MAX_HISTORY_ROUNDS = int(os.environ.get('AGENT_MAX_HISTORY_ROUNDS', '10'))
    AGENT_SQL_TIMEOUT = int(os.environ.get('AGENT_SQL_TIMEOUT', '30'))
    AGENT_MAX_RESULT_ROWS = int(os.environ.get('AGENT_MAX_RESULT_ROWS', '500'))
```

- [ ] **步骤 2：在 .env 文件中添加配置项**

在项目根目录的 `.env` 文件末尾添加：

```
# DeepSeek AI Agent
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
AGENT_MAX_HISTORY_ROUNDS=10
AGENT_SQL_TIMEOUT=30
AGENT_MAX_RESULT_ROWS=500
```

---

### 任务 3：安装依赖

**文件：**
- 修改：`requirements.txt`

- [ ] **步骤 1：添加 openai 依赖**

在 `requirements.txt` 的 `# 微信集成` 之前添加：

```
# AI Agent
openai>=1.0.0
```

- [ ] **步骤 2：安装依赖**

运行：`pip install openai>=1.0.0`
预期：Successfully installed openai-xxx

---

### 任务 4：实现 SQL 安全校验服务

**文件：**
- 创建：`app/services/agent_sql_validator.py`

- [ ] **步骤 1：编写 agent_sql_validator.py**

```python
import re
import logging

logger = logging.getLogger(__name__)

FORBIDDEN_KEYWORDS = [
    'INSERT', 'UPDATE', 'DELETE', 'DROP', 'ALTER', 'TRUNCATE',
    'EXEC', 'EXECUTE', 'MERGE', 'INTO', 'BULK', 'OPENROWSET',
    'OPENDATASOURCE', 'OPENQUERY', 'xp_', 'sp_', 'SHUTDOWN',
    'DBCC', 'BACKUP', 'RESTORE', 'RECONFIGURE', 'GRANT', 'REVOKE',
    'DENY', 'CREATE', 'DISABLE', 'ENABLE', 'KILL'
]


def validate_sql(sql: str, merchant_id: int = None) -> tuple:
    if not sql or not sql.strip():
        return False, 'SQL 不能为空'

    cleaned = _remove_comments(sql)

    first_word = cleaned.strip().split()[0].upper() if cleaned.strip() else ''
    if first_word != 'SELECT':
        return False, f'只允许 SELECT 查询，检测到: {first_word}'

    upper_cleaned = cleaned.upper()
    for keyword in FORBIDDEN_KEYWORDS:
        if keyword == 'INTO':
            if re.search(r'\bINTO\b', upper_cleaned):
                return False, f'禁止使用 INTO 关键字'
        elif keyword in ('SELECT',):
            continue
        else:
            if re.search(r'\b' + keyword + r'\b', upper_cleaned):
                return False, f'禁止使用 {keyword} 关键字'

    if ';' in cleaned:
        parts = [p.strip() for p in cleaned.split(';') if p.strip()]
        if len(parts) > 1:
            return False, '禁止执行多条 SQL 语句'

    if merchant_id is not None:
        if 'MerchantID' not in upper_cleaned:
            return False, '商户端查询必须包含 MerchantID 条件'

    cleaned = _ensure_top_limit(cleaned)

    return True, cleaned


def _remove_comments(sql: str) -> str:
    sql = re.sub(r'--.*$', '', sql, flags=re.MULTILINE)
    sql = re.sub(r'/\*.*?\*/', '', sql, flags=re.DOTALL)
    return sql.strip()


def _ensure_top_limit(sql: str) -> str:
    upper = sql.upper()
    if re.search(r'\bTOP\s+\d+', upper):
        existing = re.search(r'TOP\s+(\d+)', upper)
        if existing:
            limit = int(existing.group(1))
            if limit > 500:
                sql = re.sub(r'TOP\s+\d+', 'TOP 500', sql, flags=re.IGNORECASE)
        return sql

    select_match = re.match(r'(SELECT\s+)', sql, re.IGNORECASE)
    if select_match:
        sql = sql[:select_match.end()] + 'TOP 500 ' + sql[select_match.end():]
    return sql
```

- [ ] **步骤 2：验证模块可导入**

运行：`python -c "from app.services.agent_sql_validator import validate_sql; print(validate_sql('SELECT * FROM Merchant'))"`
预期：输出 `(True, 'SELECT TOP 500  * FROM Merchant')`

---

### 任务 5：实现 Prompt 构建服务

**文件：**
- 创建：`app/services/agent_prompt_builder.py`

- [ ] **步骤 1：编写 agent_prompt_builder.py**

```python
import logging

logger = logging.getLogger(__name__)

DATABASE_SCHEMA = """
-- 商户表
Merchant (MerchantID, MerchantName NVARCHAR, LegalPerson NVARCHAR, ContactPhone NVARCHAR, MerchantType NVARCHAR, BusinessType NVARCHAR, Status NVARCHAR, CreateTime DATETIME)
  MerchantType 取值: N'企业' / N'个体'  BusinessType 取值从 Sys_Dictionary(dict_type='business_type')  Status 取值: N'正常' / N'停业'

-- 地块/铺位表
Plot (PlotID, PlotNumber NVARCHAR, Area DECIMAL, UnitPrice DECIMAL, PlotType NVARCHAR, Status NVARCHAR)
  PlotType 取值: N'水泥' / N'钢材' / N'砖类' / N'其他'  Status 取值: N'空闲' / N'已租' / N'维修'

-- 合同表
Contract (ContractID, ContractNo NVARCHAR, MerchantID INT, TotalAmount DECIMAL, StartDate DATE, EndDate DATE, BusinessType NVARCHAR, Status NVARCHAR, CreateTime DATETIME)
  Status 取值: N'执行中' / N'已到期' / N'已终止' / N'待签约'

-- 合同-地块关联表
ContractPlot (ContractPlotID, ContractID INT, PlotID INT, UnitPrice DECIMAL)

-- 电表表
ElectricityMeter (MeterID, MeterNumber NVARCHAR, Multiplier DECIMAL, InitialReading DECIMAL, Status NVARCHAR)

-- 水表表
WaterMeter (MeterID, MeterNumber NVARCHAR, Multiplier DECIMAL, InitialReading DECIMAL, Status NVARCHAR)

-- 合同-电表关联表
ContractElectricityMeter (ContractMeterID, ContractID INT, MeterID INT, UnitPrice DECIMAL, StartReading DECIMAL, IsActive BIT)

-- 合同-水表关联表
ContractWaterMeter (ContractMeterID, ContractID INT, MeterID INT, UnitPrice DECIMAL, StartReading DECIMAL, IsActive BIT)

-- 抄表记录表
UtilityReading (ReadingID, ContractMeterID INT, PreviousReading DECIMAL, CurrentReading DECIMAL, Usage DECIMAL, Amount DECIMAL, ReadingMonth NVARCHAR, ReadingDate DATE)

-- 换表记录表
MeterChangeRecord (ChangeRecordID, ContractMeterID INT, OldMeterID INT, NewMeterID INT, OldReading DECIMAL, NewReading DECIMAL, Reason NVARCHAR, ChangeDate DATE)

-- 费用类型表
ExpenseType (ExpenseTypeID, ExpenseTypeName NVARCHAR, ExpenseTypeCode NVARCHAR, Direction NVARCHAR)
  Direction 取值: N'收入' / N'支出'

-- 应收账款表
Receivable (ReceivableID, CustomerType NVARCHAR, CustomerID INT, MerchantID INT, ExpenseTypeID INT, Amount DECIMAL, PaidAmount DECIMAL, RemainingAmount DECIMAL, DueDate DATE, Status NVARCHAR, Description NVARCHAR, CreateTime DATETIME, IsDeleted BIT)
  CustomerType 取值: N'Merchant' / N'Customer'  Status 取值: N'待付款' / N'部分付款' / N'已付款'

-- 应付账款表
Payable (PayableID, CustomerType NVARCHAR, CustomerID INT, VendorName NVARCHAR, ExpenseTypeID INT, Amount DECIMAL, PaidAmount DECIMAL, RemainingAmount DECIMAL, DueDate DATE, Status NVARCHAR, Description NVARCHAR, CreateTime DATETIME, IsDeleted BIT)
  Status 取值: N'待付款' / N'部分付款' / N'已付款'

-- 现金流水表
CashFlow (CashFlowID, Amount DECIMAL, Direction NVARCHAR, ExpenseTypeID INT, Description NVARCHAR, TransactionDate DATE, ReferenceID INT, ReferenceType NVARCHAR, CreatedBy INT, AccountID INT, TransactionNo NVARCHAR, CreateTime DATETIME)
  Direction 取值: N'收入' / N'支出'

-- 收款记录表
CollectionRecord (CollectionRecordID, ReceivableID INT, MerchantID INT, Amount DECIMAL, PaymentMethod NVARCHAR, TransactionDate DATE, Description NVARCHAR, CreatedBy INT, IsReversed BIT, CreateTime DATETIME)

-- 付款记录表
PaymentRecord (PaymentRecordID, PayableID INT, Amount DECIMAL, PaymentMethod NVARCHAR, TransactionDate DATE, Description NVARCHAR, CreatedBy INT, IsReversed BIT, CreateTime DATETIME)

-- 资金账户表
Account (AccountID, AccountName NVARCHAR, AccountType NVARCHAR, BankName NVARCHAR, BankAccount NVARCHAR, Balance DECIMAL, Status NVARCHAR)
  AccountType 取值: N'现金' / N'银行' / N'微信'  Status 取值: N'有效' / N'停用'

-- 往来客户表
Customer (CustomerID, CustomerName NVARCHAR, ContactPerson NVARCHAR, ContactPhone NVARCHAR, CustomerType NVARCHAR)
  CustomerType 取值: N'供应商' / N'服务商' / N'其他'

-- 预收/预付表
Prepayment (PrepaymentID, Direction NVARCHAR, CustomerType NVARCHAR, CustomerID INT, CustomerName NVARCHAR, TotalAmount DECIMAL, AppliedAmount DECIMAL, RemainingAmount DECIMAL, Status NVARCHAR, CreateTime DATETIME)
  Direction 取值: N'预收' / N'预付'  Status 取值: N'有效' / N'已核销' / N'已关闭'

-- 预收冲抵明细表
PrepaymentApply (ApplyID, PrepaymentID INT, TargetID INT, TargetType NVARCHAR, Amount DECIMAL, ApplyTime DATETIME)

-- 押金表
Deposit (DepositID, CustomerType NVARCHAR, CustomerID INT, CustomerName NVARCHAR, DepositType NVARCHAR, Amount DECIMAL, RemainingAmount DECIMAL, Status NVARCHAR, CreateTime DATETIME)
  DepositType 取值: N'合同押金' / N'水电押金' / N'其他'  Status 取值: N'有效' / N'已退还' / N'已扣除' / N'已转抵'

-- 押金操作记录表
DepositOperation (OperationID, DepositID INT, OperationType NVARCHAR, Amount DECIMAL, Description NVARCHAR, CreateTime DATETIME)
  OperationType 取值: N'退还' / N'扣除' / N'转抵'

-- 磅秤表
Scale (ScaleID, ScaleNumber NVARCHAR, Location NVARCHAR, Capacity DECIMAL, Status NVARCHAR)

-- 过磅记录表
ScaleRecord (ScaleRecordID, ScaleID INT, GrossWeight DECIMAL, TareWeight DECIMAL, NetWeight DECIMAL, UnitPrice DECIMAL, Amount DECIMAL, VehicleNumber NVARCHAR, ProductName NVARCHAR, RecordTime DATETIME)

-- 工资档案表
SalaryProfile (ProfileID, EmployeeName NVARCHAR, BaseSalary DECIMAL, PositionSalary DECIMAL, SocialInsurance DECIMAL, HousingFund DECIMAL)

-- 月度工资单表
SalaryRecord (RecordID, ProfileID INT, Month NVARCHAR, GrossPay DECIMAL, Deductions DECIMAL, NetPay DECIMAL, Status NVARCHAR)
  Status 取值: N'待审核' / N'已审核' / N'已发放'

-- 宿舍房间表
DormRoom (RoomID, RoomNumber NVARCHAR, RoomType NVARCHAR, MonthlyRent DECIMAL, WaterQuota DECIMAL, ElectricityUnitPrice DECIMAL, Status NVARCHAR)
  Status 取值: N'空闲' / N'已住' / N'维修'

-- 入住记录表
DormOccupancy (OccupancyID, RoomID INT, TenantName NVARCHAR, CheckInDate DATE, CheckOutDate DATE, IDNumber NVARCHAR)

-- 宿舍电表读数表
DormReading (ReadingID, RoomID INT, Month NVARCHAR, PreviousReading DECIMAL, CurrentReading DECIMAL, Usage DECIMAL)

-- 宿舍月度账单表
DormBill (BillID, OccupancyID INT, Month NVARCHAR, Rent DECIMAL, WaterFee DECIMAL, ElectricityFee DECIMAL, TotalAmount DECIMAL, Status NVARCHAR)
  Status 取值: N'待付款' / N'已付款'

-- 垃圾清运记录表
GarbageCollection (CollectionID, CollectionDate DATE, GarbageType NVARCHAR, Quantity DECIMAL, Amount DECIMAL, VendorName NVARCHAR)

-- 商户垃圾费表
GarbageFee (GarbageFeeID, MerchantID INT, Year INT, BusinessType NVARCHAR, Area DECIMAL, FeeAmount DECIMAL)

-- 费用单主表
ExpenseOrder (OrderID, OrderNo NVARCHAR, Category NVARCHAR, VendorID INT, TotalAmount DECIMAL, CreateTime DATETIME)

-- 费用单明细表
ExpenseOrderItem (ItemID, OrderID INT, ExpenseTypeID INT, Amount DECIMAL, WorkerName NVARCHAR, WorkDate DATE)

-- 冲销记录表
ReversalRecord (ReversalID, OriginalType NVARCHAR, OriginalID INT, ReversalAmount DECIMAL, Reason NVARCHAR, CreateTime DATETIME)

-- 系统字典表
Sys_Dictionary (DictID, DictType NVARCHAR, DictName NVARCHAR, DictCode NVARCHAR, SortOrder INT, Status NVARCHAR)
  常用 DictType: 'merchant_type', 'business_type', 'expense_item_income', 'expense_item_expend', 'unit_type', 'payment_method'

-- 角色表
Role (RoleID, RoleName NVARCHAR, Description NVARCHAR)

-- 权限表
Permission (PermissionID, Module NVARCHAR, Action NVARCHAR, Description NVARCHAR)

-- 角色权限关联表
RolePermission (RolePermissionID, RoleID INT, PermissionID INT)

-- 用户表
[User] (UserID, Username NVARCHAR, PasswordHash NVARCHAR, RealName NVARCHAR, Phone NVARCHAR, WechatOpenID NVARCHAR, MerchantID INT, Status NVARCHAR)

-- 用户角色关联表
UserRole (UserRoleID, UserID INT, RoleID INT)

-- 微信用户表
WxUser (WxUserID, OpenID NVARCHAR, UnionID NVARCHAR, Nickname NVARCHAR, Phone NVARCHAR, CurrentMerchantID INT)

-- 商户绑定申请表
MerchantBinding (BindingID, WxUserID INT, MerchantID INT, Role NVARCHAR, Status NVARCHAR, ApplyTime DATETIME, AuditTime DATETIME)
  Role 取值: N'Boss' / N'Staff' / N'Finance'  Status 取值: N'待审核' / N'已通过' / N'已拒绝'
"""


def build_system_prompt(source: str = 'admin', merchant_id: int = None, merchant_name: str = None) -> str:
    prompt = f"""你是宏发金属交易市场的数据查询助手。你的职责是根据用户的问题，生成 SQL 查询语句从数据库中获取数据，然后用自然语言回答用户的问题。

## 数据库结构

以下是数据库中所有表的结构（SQL Server）：

{DATABASE_SCHEMA}

## 重要说明

1. 表名和字段名区分大小写，请严格按照上面给出的名称使用
2. 中文值必须使用 N 前缀，例如：N'已签约'、N'执行中'
3. 日期比较使用标准格式，例如：WHERE EndDate >= CAST(GETDATE() AS DATE)
4. 本月范围：WHERE CreateTime >= DATEADD(MONTH, DATEDIFF(MONTH, 0, GETDATE()), 0)
5. 金额字段（Amount、PaidAmount 等）是 DECIMAL 类型
6. 状态字段是 NVARCHAR 类型，使用中文值
7. 用户表名是 [User]，查询时需要用方括号括起来

## SQL 生成规则

1. 只允许生成 SELECT 查询语句
2. 禁止 INSERT、UPDATE、DELETE、DROP、ALTER 等写操作
3. 必须使用 TOP 限制结果数量，最多 TOP 500
4. 对于聚合查询（SUM、COUNT 等），不需要 TOP
5. 使用中文别名让结果更易读，例如：SELECT SUM(Amount) AS 总金额
6. 多表关联时使用 JOIN，注意关联条件
7. 如果用户的问题模糊，生成最合理的查询并说明假设

## 回答格式

当需要查询数据时，请按以下格式回答：

1. 先给出 SQL 查询（用 ```sql ``` 包裹）
2. 等待查询结果后，再用自然语言总结回答

如果用户的问题不需要查询数据库（如闲聊、澄清问题），直接回答即可，不需要生成 SQL。
"""

    if source == 'wx' and merchant_id is not None:
        prompt += f"""

## 商户端权限约束

当前用户是商户「{merchant_name}」（MerchantID = {merchant_id}）的工作人员。
所有涉及商户数据的查询，必须包含 WHERE MerchantID = {merchant_id} 条件。
这是强制要求，不能省略。如果用户问的问题涉及其他商户的数据，告知你没有权限查看。
"""

    return prompt
```

- [ ] **步骤 2：验证模块可导入**

运行：`python -c "from app.services.agent_prompt_builder import build_system_prompt; p = build_system_prompt(); print(len(p), 'chars')"`
预期：输出字符数，无报错

---

### 任务 6：实现 Agent 核心服务

**文件：**
- 创建：`app/services/agent_service.py`

- [ ] **步骤 1：编写 agent_service.py**

```python
import logging
import re
import json
from datetime import datetime
from openai import OpenAI
from flask import current_app
from utils.database import DBConnection
from app.services.agent_sql_validator import validate_sql
from app.services.agent_prompt_builder import build_system_prompt

logger = logging.getLogger(__name__)


class AgentService:

    def chat(self, user_id, conversation_id, message, source='admin', merchant_id=None, merchant_name=None):
        if not conversation_id:
            conversation_id = self._create_conversation(user_id, message, source)

        self._save_message(conversation_id, 'user', message)

        history = self._load_history(conversation_id)
        system_prompt = build_system_prompt(source, merchant_id, merchant_name)

        messages = [{'role': 'system', 'content': system_prompt}]
        for msg in history:
            messages.append({'role': msg['role'], 'content': msg['content']})
        messages.append({'role': 'user', 'content': message})

        first_reply = self._call_llm(messages)

        sql_blocks = self._extract_sql(first_reply)

        if sql_blocks:
            sql = sql_blocks[0]
            is_valid, result = validate_sql(sql, merchant_id=merchant_id if source == 'wx' else None)

            if not is_valid:
                reply_content = f"生成的 SQL 未通过安全校验：{result}。请换个方式描述你的问题。"
                self._save_message(conversation_id, 'assistant', reply_content, generated_sql=sql)
                return {
                    'conversation_id': conversation_id,
                    'content': reply_content,
                    'generated_sql': sql,
                    'query_result': None
                }

            clean_sql = result
            query_result = self._execute_sql(clean_sql)

            if query_result is None:
                reply_content = "查询执行失败，请稍后重试或换个方式描述你的问题。"
                self._save_message(conversation_id, 'assistant', reply_content, generated_sql=clean_sql)
                return {
                    'conversation_id': conversation_id,
                    'content': reply_content,
                    'generated_sql': clean_sql,
                    'query_result': None
                }

            result_text = self._format_query_result(query_result)

            messages.append({'role': 'assistant', 'content': first_reply})
            messages.append({'role': 'user', 'content': f'查询结果如下：\n\n{result_text}\n\n请根据以上查询结果，用自然语言回答用户的问题。回答要简洁明了，包含关键数据和金额。'})

            final_reply = self._call_llm(messages)

            self._save_message(conversation_id, 'assistant', final_reply, generated_sql=clean_sql, query_result=result_text)
            return {
                'conversation_id': conversation_id,
                'content': final_reply,
                'generated_sql': clean_sql,
                'query_result': result_text
            }
        else:
            self._save_message(conversation_id, 'assistant', first_reply)
            return {
                'conversation_id': conversation_id,
                'content': first_reply,
                'generated_sql': None,
                'query_result': None
            }

    def get_conversations(self, user_id, source='admin'):
        with DBConnection() as conn:
            cursor = conn.cursor()
            cursor.execute("""
                SELECT ConversationID, Title, Source, CreateTime, UpdateTime
                FROM AgentConversation
                WHERE UserID = ? AND Source = ?
                ORDER BY UpdateTime DESC
            """, (user_id, source))
            rows = cursor.fetchall()
            result = []
            for row in rows:
                result.append({
                    'conversation_id': row.ConversationID,
                    'title': row.Title,
                    'source': row.Source,
                    'create_time': row.CreateTime.strftime('%Y-%m-%d %H:%M') if row.CreateTime else '',
                    'update_time': row.UpdateTime.strftime('%Y-%m-%d %H:%M') if row.UpdateTime else ''
                })
            return result

    def get_history(self, conversation_id, user_id):
        with DBConnection() as conn:
            cursor = conn.cursor()
            cursor.execute("""
                SELECT ConversationID FROM AgentConversation
                WHERE ConversationID = ? AND UserID = ?
            """, (conversation_id, user_id))
            if not cursor.fetchone():
                return []

            cursor.execute("""
                SELECT MessageID, Role, Content, GeneratedSQL, QueryResult, CreateTime
                FROM AgentMessage
                WHERE ConversationID = ?
                ORDER BY CreateTime ASC
            """, (conversation_id,))
            rows = cursor.fetchall()
            result = []
            for row in rows:
                item = {
                    'message_id': row.MessageID,
                    'role': row.Role,
                    'content': row.Content,
                    'generated_sql': row.GeneratedSQL,
                    'query_result': row.QueryResult,
                    'create_time': row.CreateTime.strftime('%Y-%m-%d %H:%M') if row.CreateTime else ''
                }
                result.append(item)
            return result

    def create_conversation(self, user_id, source='admin'):
        return self._create_conversation(user_id, N'新对话', source)

    def delete_conversation(self, conversation_id, user_id):
        with DBConnection() as conn:
            cursor = conn.cursor()
            cursor.execute("""
                SELECT ConversationID FROM AgentConversation
                WHERE ConversationID = ? AND UserID = ?
            """, (conversation_id, user_id))
            if not cursor.fetchone():
                return False

            cursor.execute("DELETE FROM AgentMessage WHERE ConversationID = ?", (conversation_id,))
            cursor.execute("DELETE FROM AgentConversation WHERE ConversationID = ?", (conversation_id,))
            conn.commit()
            return True

    def _create_conversation(self, user_id, title, source):
        with DBConnection() as conn:
            cursor = conn.cursor()
            cursor.execute("""
                INSERT INTO AgentConversation (UserID, Title, Source)
                OUTPUT INSERTED.ConversationID
                VALUES (?, ?, ?)
            """, (user_id, title[:200] if title else N'新对话', source))
            conversation_id = cursor.fetchone()[0]
            conn.commit()
            return conversation_id

    def _save_message(self, conversation_id, role, content, generated_sql=None, query_result=None):
        with DBConnection() as conn:
            cursor = conn.cursor()
            cursor.execute("""
                INSERT INTO AgentMessage (ConversationID, Role, Content, GeneratedSQL, QueryResult)
                VALUES (?, ?, ?, ?, ?)
            """, (conversation_id, role, content, generated_sql, query_result))
            cursor.execute("""
                UPDATE AgentConversation SET UpdateTime = GETDATE(),
                    Title = ISNULL(NULLIF(Title, N'新对话'), ?)
                WHERE ConversationID = ? AND Title = N'新对话'
            """, (content[:200] if role == 'user' else None, conversation_id))
            conn.commit()

    def _load_history(self, conversation_id):
        max_rounds = current_app.config.get('AGENT_MAX_HISTORY_ROUNDS', 10)
        with DBConnection() as conn:
            cursor = conn.cursor()
            cursor.execute("""
                SELECT TOP (?) Role, Content
                FROM AgentMessage
                WHERE ConversationID = ?
                ORDER BY CreateTime DESC
            """, (max_rounds * 2, conversation_id))
            rows = cursor.fetchall()
            return [{'role': row.Role, 'content': row.Content} for row in reversed(rows)]

    def _call_llm(self, messages):
        api_key = current_app.config.get('DEEPSEEK_API_KEY', '')
        base_url = current_app.config.get('DEEPSEEK_BASE_URL', 'https://api.deepseek.com')
        model = current_app.config.get('DEEPSEEK_MODEL', 'deepseek-chat')

        if not api_key:
            return 'AI 助手未配置，请联系管理员设置 DEEPSEEK_API_KEY。'

        try:
            client = OpenAI(api_key=api_key, base_url=base_url)
            response = client.chat.completions.create(
                model=model,
                messages=messages,
                temperature=0.1,
                max_tokens=2000
            )
            return response.choices[0].message.content
        except Exception as e:
            logger.error(f'DeepSeek API 调用失败: {e}')
            return f'AI 服务暂时不可用，请稍后重试。错误：{str(e)}'

    def _extract_sql(self, text):
        pattern = r'```sql\s*(.*?)\s*```'
        matches = re.findall(pattern, text, re.DOTALL | re.IGNORECASE)
        return [m.strip() for m in matches if m.strip()]

    def _execute_sql(self, sql):
        timeout = current_app.config.get('AGENT_SQL_TIMEOUT', 30)
        try:
            with DBConnection() as conn:
                conn.timeout = timeout
                cursor = conn.cursor()
                cursor.execute(sql)
                columns = [desc[0] for desc in cursor.description] if cursor.description else []
                rows = cursor.fetchall()
                result = []
                for row in rows:
                    row_dict = {}
                    for i, col in enumerate(columns):
                        val = row[i]
                        if isinstance(val, datetime):
                            val = val.strftime('%Y-%m-%d %H:%M')
                        elif isinstance(val, float):
                            val = round(val, 2)
                        row_dict[col] = val
                    result.append(row_dict)
                return result
        except Exception as e:
            logger.error(f'SQL 执行失败: {sql}\n错误: {e}')
            return None

    def _format_query_result(self, result):
        if not result:
            return '查询结果为空。'

        if len(result) == 1:
            parts = []
            for k, v in result[0].items():
                parts.append(f'{k}: {v}')
            return '\n'.join(parts)

        columns = list(result[0].keys())
        header = ' | '.join(columns)
        separator = ' | '.join(['---'] * len(columns))
        rows = []
        for row in result[:50]:
            rows.append(' | '.join([str(row.get(c, '')) for c in columns]))
        text = f'{header}\n{separator}\n' + '\n'.join(rows)
        if len(result) > 50:
            text += f'\n\n（共 {len(result)} 条记录，仅显示前 50 条）'
        return text
```

- [ ] **步骤 2：验证模块可导入**

运行：`python -c "from app.services.agent_service import AgentService; print('AgentService imported')"`
预期：输出 `AgentService imported`

---

### 任务 7：实现 Agent 路由

**文件：**
- 创建：`app/routes/agent.py`

- [ ] **步骤 1：编写 agent.py**

```python
from flask import Blueprint, render_template, request
from flask_login import login_required, current_user
from app.api_response import success_response, error_response, handle_exception
from app.services.agent_service import AgentService

agent_bp = Blueprint('agent', __name__)
agent_svc = AgentService()


@agent_bp.route('/chat')
@login_required
def chat():
    return render_template('agent/chat.html')


@agent_bp.route('/chat/send', methods=['POST'])
@login_required
def chat_send():
    try:
        data = request.json
        conversation_id = data.get('conversation_id')
        message = data.get('message', '').strip()

        if not message:
            return error_response('消息不能为空')

        result = agent_svc.chat(
            user_id=current_user.user_id,
            conversation_id=conversation_id,
            message=message,
            source='admin'
        )
        return success_response(result)
    except Exception as e:
        return handle_exception(e)


@agent_bp.route('/conversations', methods=['GET'])
@login_required
def conversations():
    try:
        result = agent_svc.get_conversations(current_user.user_id, source='admin')
        return success_response(result)
    except Exception as e:
        return handle_exception(e)


@agent_bp.route('/conversation/create', methods=['POST'])
@login_required
def conversation_create():
    try:
        conversation_id = agent_svc.create_conversation(current_user.user_id, source='admin')
        return success_response({'conversation_id': conversation_id})
    except Exception as e:
        return handle_exception(e)


@agent_bp.route('/conversation/delete/<int:conversation_id>', methods=['POST'])
@login_required
def conversation_delete(conversation_id):
    try:
        result = agent_svc.delete_conversation(conversation_id, current_user.user_id)
        if result:
            return success_response(message='删除成功')
        return error_response('会话不存在或无权删除')
    except Exception as e:
        return handle_exception(e)


@agent_bp.route('/history/<int:conversation_id>', methods=['GET'])
@login_required
def history(conversation_id):
    try:
        result = agent_svc.get_history(conversation_id, current_user.user_id)
        return success_response(result)
    except Exception as e:
        return handle_exception(e)


@agent_bp.route('/wx/chat')
def wx_chat():
    from flask import session
    wx_user_id = session.get('wx_user_id')
    if not wx_user_id:
        from flask import redirect, url_for
        return redirect(url_for('wx.index'))
    return render_template('agent/wx_chat.html')


@agent_bp.route('/wx/chat/send', methods=['POST'])
def wx_chat_send():
    from flask import session
    wx_user_id = session.get('wx_user_id')
    if not wx_user_id:
        return error_response('请先登录', status=401)

    try:
        data = request.json
        conversation_id = data.get('conversation_id')
        message = data.get('message', '').strip()

        if not message:
            return error_response('消息不能为空')

        from app.services.wx_bind_service import WxBindService
        bind_svc = WxBindService()
        binding = bind_svc.get_active_binding(wx_user_id)
        merchant_id = binding.get('merchant_id') if binding else None
        merchant_name = binding.get('merchant_name') if binding else None

        result = agent_svc.chat(
            user_id=wx_user_id,
            conversation_id=conversation_id,
            message=message,
            source='wx',
            merchant_id=merchant_id,
            merchant_name=merchant_name
        )
        return success_response(result)
    except Exception as e:
        return handle_exception(e)


@agent_bp.route('/wx/history/<int:conversation_id>', methods=['GET'])
def wx_history(conversation_id):
    from flask import session
    wx_user_id = session.get('wx_user_id')
    if not wx_user_id:
        return error_response('请先登录', status=401)

    try:
        result = agent_svc.get_history(conversation_id, wx_user_id)
        return success_response(result)
    except Exception as e:
        return handle_exception(e)
```

- [ ] **步骤 2：验证模块可导入**

运行：`python -c "from app.routes.agent import agent_bp; print('agent_bp imported')"`
预期：输出 `agent_bp imported`

---

### 任务 8：注册蓝图

**文件：**
- 修改：`app/__init__.py`

- [ ] **步骤 1：在 blueprints 列表中添加 agent**

在 `app/__init__.py` 的 `blueprints` 列表中，在 `('app.routes.admin', 'admin_bp', None, False)` 之前添加：

```python
        ('app.routes.agent', 'agent_bp', '/agent', False),
```

- [ ] **步骤 2：验证应用启动**

运行：`python -c "from app import create_app; app = create_app(); print([bp.name for bp in app.blueprints.values()])"`
预期：输出中包含 `'agent'`

---

### 任务 9：后台 AI 助手前端页面

**文件：**
- 创建：`templates/agent/chat.html`

- [ ] **步骤 1：编写 chat.html**

页面继承 `admin_base.html`，布局为：
- 左侧：会话列表（可新建/删除/切换）
- 右侧：对话区域（消息列表 + 底部输入框）
- 消息支持 Markdown 渲染，SQL 可折叠展示
- 使用 marked.js 做 Markdown 渲染
- 使用 admin.js 中的 ajaxPost / ajaxGet 封装

```html
{% extends "admin_base.html" %}

{% block title %}AI 助手{% endblock %}
{% block breadcrumb %}
<span class="breadcrumb-home"><i class="fas fa-home"></i></span>
<span class="breadcrumb-chip">AI 助手</span>
<span class="breadcrumb-current">数据查询</span>
{% endblock %}

{% block extra_css %}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/marked-highlight/styles/github.min.css">
<style>
.agent-container { display: flex; height: calc(100vh - 120px); background: var(--color-bg); border-radius: var(--radius-lg); overflow: hidden; box-shadow: var(--shadow-sm); }
.agent-sidebar { width: 260px; background: var(--color-bg-card); border-right: 1px solid var(--color-border); display: flex; flex-direction: column; }
.agent-sidebar-header { padding: 16px; border-bottom: 1px solid var(--color-border); }
.agent-sidebar-header .btn { width: 100%; }
.agent-conversation-list { flex: 1; overflow-y: auto; padding: 8px; }
.agent-conversation-item { padding: 10px 12px; border-radius: var(--radius-md); cursor: pointer; margin-bottom: 4px; transition: background var(--transition-fast); display: flex; justify-content: space-between; align-items: center; }
.agent-conversation-item:hover { background: var(--color-bg); }
.agent-conversation-item.active { background: var(--color-primary); color: #fff; }
.agent-conversation-item.active .conv-time { color: rgba(255,255,255,0.7); }
.agent-conversation-item .conv-title { font-size: 14px; font-weight: 500; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; flex: 1; }
.agent-conversation-item .conv-time { font-size: 11px; color: var(--color-text-muted); white-space: nowrap; margin-left: 8px; }
.agent-conversation-item .conv-delete { opacity: 0; transition: opacity var(--transition-fast); color: var(--color-text-muted); margin-left: 4px; }
.agent-conversation-item:hover .conv-delete { opacity: 1; }
.agent-conversation-item.active .conv-delete { color: rgba(255,255,255,0.7); }
.agent-main { flex: 1; display: flex; flex-direction: column; }
.agent-messages { flex: 1; overflow-y: auto; padding: 24px; }
.agent-welcome { display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100%; color: var(--color-text-muted); }
.agent-welcome i { font-size: 48px; margin-bottom: 16px; color: var(--color-primary); }
.agent-welcome h3 { color: var(--color-text); margin-bottom: 8px; }
.agent-welcome p { font-size: 14px; max-width: 400px; text-align: center; line-height: 1.6; }
.msg { margin-bottom: 20px; display: flex; }
.msg-user { justify-content: flex-end; }
.msg-avatar { width: 36px; height: 36px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 14px; flex-shrink: 0; }
.msg-user .msg-avatar { background: var(--color-primary); color: #fff; margin-left: 12px; order: 2; }
.msg-assistant .msg-avatar { background: var(--color-accent); color: #fff; margin-right: 12px; }
.msg-bubble { max-width: 70%; padding: 12px 16px; border-radius: var(--radius-lg); font-size: 14px; line-height: 1.7; }
.msg-user .msg-bubble { background: var(--color-primary); color: #fff; border-bottom-right-radius: 4px; }
.msg-assistant .msg-bubble { background: var(--color-bg-card); border: 1px solid var(--color-border); border-bottom-left-radius: 4px; }
.msg-bubble p { margin: 0 0 8px; }
.msg-bubble p:last-child { margin-bottom: 0; }
.msg-bubble ul, .msg-bubble ol { padding-left: 20px; margin: 8px 0; }
.msg-bubble code { background: rgba(0,0,0,0.06); padding: 2px 6px; border-radius: 4px; font-family: var(--font-mono); font-size: 13px; }
.msg-bubble pre { background: #1e1e2e; color: #cdd6f4; padding: 12px 16px; border-radius: var(--radius-md); overflow-x: auto; margin: 8px 0; position: relative; }
.msg-bubble pre code { background: none; padding: 0; color: inherit; font-size: 13px; }
.sql-toggle { position: absolute; top: 8px; right: 8px; background: rgba(255,255,255,0.1); border: none; color: #cdd6f4; padding: 4px 8px; border-radius: 4px; cursor: pointer; font-size: 11px; }
.sql-toggle:hover { background: rgba(255,255,255,0.2); }
.agent-input-area { padding: 16px 24px; border-top: 1px solid var(--color-border); background: var(--color-bg-card); }
.agent-input-wrap { display: flex; gap: 12px; align-items: flex-end; }
.agent-input-wrap textarea { flex: 1; border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: 10px 14px; font-size: 14px; resize: none; min-height: 44px; max-height: 120px; font-family: var(--font-body); background: var(--color-bg); color: var(--color-text); }
.agent-input-wrap textarea:focus { outline: none; border-color: var(--color-primary); box-shadow: 0 0 0 3px rgba(22,93,255,0.1); }
.agent-input-wrap .btn-send { width: 44px; height: 44px; border-radius: var(--radius-md); display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
.msg-loading { display: flex; gap: 4px; padding: 8px 0; }
.msg-loading span { width: 8px; height: 8px; border-radius: 50%; background: var(--color-text-muted); animation: bounce 1.4s infinite ease-in-out both; }
.msg-loading span:nth-child(1) { animation-delay: -0.32s; }
.msg-loading span:nth-child(2) { animation-delay: -0.16s; }
@keyframes bounce { 0%, 80%, 100% { transform: scale(0); } 40% { transform: scale(1); } }
.suggestion-chips { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 16px; }
.suggestion-chip { padding: 6px 14px; border-radius: 20px; background: var(--color-bg); border: 1px solid var(--color-border); font-size: 13px; cursor: pointer; transition: all var(--transition-fast); }
.suggestion-chip:hover { border-color: var(--color-primary); color: var(--color-primary); }
</style>
{% endblock %}

{% block content %}
<div class="agent-container">
    <div class="agent-sidebar">
        <div class="agent-sidebar-header">
            <button class="btn btn-primary" onclick="createConversation()">
                <i class="fas fa-plus me-1"></i> 新建对话
            </button>
        </div>
        <div class="agent-conversation-list" id="conversationList"></div>
    </div>
    <div class="agent-main">
        <div class="agent-messages" id="messageArea">
            <div class="agent-welcome" id="welcomeArea">
                <i class="fas fa-robot"></i>
                <h3>AI 数据助手</h3>
                <p>我可以帮你查询业务数据，比如应付账款、合同到期、水电费等。直接用自然语言提问即可。</p>
                <div class="suggestion-chips">
                    <span class="suggestion-chip" onclick="sendSuggestion(this)">本月应付账款有多少？</span>
                    <span class="suggestion-chip" onclick="sendSuggestion(this)">本月合同到期的有哪些？</span>
                    <span class="suggestion-chip" onclick="sendSuggestion(this)">今年水电费收入总计多少？</span>
                    <span class="suggestion-chip" onclick="sendSuggestion(this)">当前空闲地块有哪些？</span>
                </div>
            </div>
        </div>
        <div class="agent-input-area">
            <div class="agent-input-wrap">
                <textarea id="messageInput" placeholder="输入你的问题..." rows="1" onkeydown="handleKeydown(event)"></textarea>
                <button class="btn btn-primary btn-send" onclick="sendMessage()" id="sendBtn">
                    <i class="fas fa-paper-plane"></i>
                </button>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
<script>
let currentConversationId = null;
let isLoading = false;

const textarea = document.getElementById('messageInput');
textarea.addEventListener('input', function() {
    this.style.height = 'auto';
    this.style.height = Math.min(this.scrollHeight, 120) + 'px';
});

function handleKeydown(e) {
    if (e.key === 'Enter' && !e.shiftKey) {
        e.preventDefault();
        sendMessage();
    }
}

function sendSuggestion(el) {
    document.getElementById('messageInput').value = el.textContent;
    sendMessage();
}

function loadConversations() {
    ajaxGet('/agent/conversations', {}, function(res) {
        const list = document.getElementById('conversationList');
        list.innerHTML = '';
        (res.data || []).forEach(function(conv) {
            const item = document.createElement('div');
            item.className = 'agent-conversation-item' + (conv.conversation_id === currentConversationId ? ' active' : '');
            item.innerHTML = `
                <span class="conv-title">${escapeHtml(conv.title)}</span>
                <span class="conv-time">${conv.update_time.split(' ')[0].slice(5)}</span>
                <span class="conv-delete" onclick="event.stopPropagation(); deleteConversation(${conv.conversation_id})"><i class="fas fa-times"></i></span>
            `;
            item.onclick = function() { switchConversation(conv.conversation_id); };
            list.appendChild(item);
        });
    });
}

function createConversation() {
    ajaxPost('/agent/conversation/create', {}, function(res) {
        currentConversationId = res.data.conversation_id;
        document.getElementById('messageArea').innerHTML = document.getElementById('welcomeArea') ? document.getElementById('welcomeArea').outerHTML : '';
        loadConversations();
    });
}

function deleteConversation(id) {
    if (!confirm('确定删除此对话？')) return;
    ajaxPost('/agent/conversation/delete/' + id, {}, function(res) {
        if (currentConversationId === id) {
            currentConversationId = null;
            location.reload();
        }
        loadConversations();
    });
}

function switchConversation(id) {
    currentConversationId = id;
    loadConversations();
    loadHistory(id);
}

function loadHistory(id) {
    ajaxGet('/agent/history/' + id, {}, function(res) {
        const area = document.getElementById('messageArea');
        area.innerHTML = '';
        (res.data || []).forEach(function(msg) {
            appendMessage(msg.role, msg.content, msg.generated_sql);
        });
        scrollToBottom();
    });
}

function sendMessage() {
    if (isLoading) return;
    const input = document.getElementById('messageInput');
    const message = input.value.trim();
    if (!message) return;

    if (!currentConversationId) {
        ajaxPost('/agent/conversation/create', {}, function(res) {
            currentConversationId = res.data.conversation_id;
            loadConversations();
            doSend(message);
        });
        return;
    }
    doSend(message);
}

function doSend(message) {
    const input = document.getElementById('messageInput');
    input.value = '';
    input.style.height = 'auto';

    appendMessage('user', message);
    showLoading();

    isLoading = true;
    document.getElementById('sendBtn').disabled = true;

    ajaxPost('/agent/chat/send', {
        conversation_id: currentConversationId,
        message: message
    }, function(res) {
        hideLoading();
        isLoading = false;
        document.getElementById('sendBtn').disabled = false;

        if (res.data && res.data.content) {
            appendMessage('assistant', res.data.content, res.data.generated_sql);
            if (res.data.conversation_id) {
                currentConversationId = res.data.conversation_id;
            }
            loadConversations();
        }
        scrollToBottom();
    }, function(err) {
        hideLoading();
        isLoading = false;
        document.getElementById('sendBtn').disabled = false;
        appendMessage('assistant', '抱歉，请求处理失败，请稍后重试。');
        scrollToBottom();
    });
}

function appendMessage(role, content, sql) {
    const area = document.getElementById('messageArea');
    const welcome = document.getElementById('welcomeArea');
    if (welcome) welcome.remove();

    const div = document.createElement('div');
    div.className = 'msg msg-' + role;

    const avatar = document.createElement('div');
    avatar.className = 'msg-avatar';
    avatar.innerHTML = role === 'user' ? '<i class="fas fa-user"></i>' : '<i class="fas fa-robot"></i>';

    const bubble = document.createElement('div');
    bubble.className = 'msg-bubble';

    if (role === 'assistant') {
        let htmlContent = marked.parse(content || '');
        if (sql) {
            const sqlId = 'sql-' + Date.now();
            htmlContent += `<pre id="${sqlId}"><button class="sql-toggle" onclick="toggleSql('${sqlId}')">SQL</button><code class="language-sql">${escapeHtml(sql)}</code></pre>`;
        }
        bubble.innerHTML = htmlContent;
    } else {
        bubble.textContent = content;
    }

    div.appendChild(avatar);
    div.appendChild(bubble);
    area.appendChild(div);
}

function toggleSql(id) {
    const pre = document.getElementById(id);
    const code = pre.querySelector('code');
    const btn = pre.querySelector('.sql-toggle');
    if (code.style.display === 'none') {
        code.style.display = '';
        btn.textContent = 'SQL';
    } else {
        code.style.display = 'none';
        btn.textContent = '展开 SQL';
    }
}

function showLoading() {
    const area = document.getElementById('messageArea');
    const div = document.createElement('div');
    div.className = 'msg msg-assistant';
    div.id = 'loadingMsg';
    div.innerHTML = `
        <div class="msg-avatar"><i class="fas fa-robot"></i></div>
        <div class="msg-bubble">
            <div class="msg-loading"><span></span><span></span><span></span></div>
        </div>
    `;
    area.appendChild(div);
    scrollToBottom();
}

function hideLoading() {
    const el = document.getElementById('loadingMsg');
    if (el) el.remove();
}

function scrollToBottom() {
    const area = document.getElementById('messageArea');
    area.scrollTop = area.scrollHeight;
}

function escapeHtml(str) {
    if (!str) return '';
    const div = document.createElement('div');
    div.textContent = str;
    return div.innerHTML;
}

loadConversations();
</script>
{% endblock %}
```

- [ ] **步骤 2：验证模板文件存在**

确认 `templates/agent/chat.html` 文件已创建。

---

### 任务 10：微信端 AI 助手页面

**文件：**
- 创建：`templates/agent/wx_chat.html`

- [ ] **步骤 1：编写 wx_chat.html**

页面继承 `wx_base.html`，简洁对话界面：
- 单会话模式
- 底部输入框 + 发送按钮
- 消息气泡展示

```html
{% extends "wx_base.html" %}

{% block title %}智能客服{% endblock %}

{% block content %}
<div style="display:flex;flex-direction:column;height:calc(100vh - 120px);padding:0;">
    <div id="messageArea" style="flex:1;overflow-y:auto;padding:16px;">
        <div id="welcomeArea" style="text-align:center;padding:40px 20px;color:var(--color-text-muted);">
            <i class="fas fa-robot" style="font-size:36px;color:var(--color-primary);margin-bottom:12px;display:block;"></i>
            <h4 style="color:var(--color-text);margin-bottom:8px;">智能客服</h4>
            <p style="font-size:13px;line-height:1.6;">我可以帮你查询合同、账单、水电费等信息，直接提问即可。</p>
        </div>
    </div>
    <div style="padding:12px 16px;border-top:1px solid var(--color-border);background:var(--color-bg-card);">
        <div style="display:flex;gap:8px;align-items:flex-end;">
            <textarea id="messageInput" placeholder="输入你的问题..." rows="1" style="flex:1;border:1px solid var(--color-border);border-radius:8px;padding:8px 12px;font-size:14px;resize:none;min-height:38px;max-height:100px;font-family:inherit;background:var(--color-bg);color:var(--color-text);"></textarea>
            <button class="wx-btn" onclick="sendMessage()" id="sendBtn" style="flex-shrink:0;height:38px;padding:0 16px;">
                <i class="fas fa-paper-plane"></i>
            </button>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
<script>
let currentConversationId = null;
let isLoading = false;

const textarea = document.getElementById('messageInput');
textarea.addEventListener('input', function() {
    this.style.height = 'auto';
    this.style.height = Math.min(this.scrollHeight, 100) + 'px';
});
textarea.addEventListener('keydown', function(e) {
    if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendMessage(); }
});

function sendMessage() {
    if (isLoading) return;
    const input = document.getElementById('messageInput');
    const message = input.value.trim();
    if (!message) return;
    input.value = '';
    input.style.height = 'auto';

    appendMessage('user', message);
    showLoading();
    isLoading = true;

    wxPost('/agent/wx/chat/send', {
        conversation_id: currentConversationId,
        message: message
    }, function(res) {
        hideLoading();
        isLoading = false;
        if (res.data && res.data.content) {
            appendMessage('assistant', res.data.content, res.data.generated_sql);
            if (res.data.conversation_id) currentConversationId = res.data.conversation_id;
        }
        scrollToBottom();
    }, function() {
        hideLoading();
        isLoading = false;
        appendMessage('assistant', '请求失败，请稍后重试。');
    });
}

function appendMessage(role, content, sql) {
    const area = document.getElementById('messageArea');
    const welcome = document.getElementById('welcomeArea');
    if (welcome) welcome.remove();

    const div = document.createElement('div');
    div.style.cssText = 'margin-bottom:12px;display:flex;' + (role === 'user' ? 'justify-content:flex-end;' : '');

    const bubble = document.createElement('div');
    bubble.style.cssText = 'max-width:80%;padding:10px 14px;border-radius:12px;font-size:14px;line-height:1.6;' +
        (role === 'user'
            ? 'background:var(--color-primary);color:#fff;border-bottom-right-radius:4px;'
            : 'background:var(--color-bg-card);border:1px solid var(--color-border);border-bottom-left-radius:4px;');

    if (role === 'assistant') {
        bubble.innerHTML = marked.parse(content || '');
        if (sql) {
            const sqlId = 'sql-' + Date.now();
            bubble.innerHTML += `<pre id="${sqlId}" style="background:#1e1e2e;color:#cdd6f4;padding:8px 12px;border-radius:6px;overflow-x:auto;margin:8px 0;font-size:12px;position:relative;"><button onclick="toggleSql('${sqlId}')" style="position:absolute;top:4px;right:4px;background:rgba(255,255,255,0.1);border:none;color:#cdd6f4;padding:2px 6px;border-radius:4px;cursor:pointer;font-size:10px;">SQL</button><code>${escapeHtml(sql)}</code></pre>`;
        }
    } else {
        bubble.textContent = content;
    }

    div.appendChild(bubble);
    area.appendChild(div);
}

function toggleSql(id) {
    const pre = document.getElementById(id);
    const code = pre.querySelector('code');
    const btn = pre.querySelector('button');
    code.style.display = code.style.display === 'none' ? '' : 'none';
    btn.textContent = code.style.display === 'none' ? '展开' : 'SQL';
}

function showLoading() {
    const area = document.getElementById('messageArea');
    const div = document.createElement('div');
    div.id = 'loadingMsg';
    div.style.cssText = 'margin-bottom:12px;display:flex;';
    div.innerHTML = '<div style="padding:10px 14px;border-radius:12px;background:var(--color-bg-card);border:1px solid var(--color-border);border-bottom-left-radius:4px;"><span style="display:inline-block;width:6px;height:6px;border-radius:50%;background:var(--color-text-muted);animation:bounce 1.4s infinite ease-in-out both;margin:0 2px;"></span><span style="display:inline-block;width:6px;height:6px;border-radius:50%;background:var(--color-text-muted);animation:bounce 1.4s infinite ease-in-out both;margin:0 2px;animation-delay:0.16s;"></span><span style="display:inline-block;width:6px;height:6px;border-radius:50%;background:var(--color-text-muted);animation:bounce 1.4s infinite ease-in-out both;margin:0 2px;animation-delay:0.32s;"></span></div>';
    area.appendChild(div);
    scrollToBottom();
}

function hideLoading() {
    const el = document.getElementById('loadingMsg');
    if (el) el.remove();
}

function scrollToBottom() {
    const area = document.getElementById('messageArea');
    area.scrollTop = area.scrollHeight;
}

function escapeHtml(str) {
    if (!str) return '';
    const div = document.createElement('div');
    div.textContent = str;
    return div.innerHTML;
}
</script>
{% endblock %}
```

---

### 任务 11：侧边栏添加 AI 助手菜单

**文件：**
- 修改：`templates/admin_base.html`

- [ ] **步骤 1：在侧边栏菜单中添加 AI 助手入口**

在 `templates/admin_base.html` 的侧边栏菜单中，在"工作台"菜单项之后，添加 AI 助手菜单项：

```html
<li class="sidebar-item">
    <a href="{{ url_for('agent.chat') }}" class="sidebar-link">
        <i class="fas fa-robot"></i>
        <span>AI 助手</span>
    </a>
</li>
```

具体位置：搜索 `工作台` 或 `admin.index`，在其对应的 `<li>` 之后插入。

---

### 任务 12：微信端添加智能客服入口

**文件：**
- 修改：`templates/wx/index.html`

- [ ] **步骤 1：在微信门户首页添加智能客服入口**

在微信门户首页的统计卡片区域之后，添加智能客服入口卡片：

```html
<a href="{{ url_for('agent.wx_chat') }}" style="display:block;margin:16px;padding:16px;background:linear-gradient(135deg, var(--color-primary), #4f8eff);border-radius:12px;color:#fff;text-decoration:none;">
    <div style="display:flex;align-items:center;gap:12px;">
        <i class="fas fa-robot" style="font-size:24px;"></i>
        <div>
            <div style="font-weight:600;font-size:15px;">智能客服</div>
            <div style="font-size:12px;opacity:0.8;">查询合同、账单、水电费等</div>
        </div>
        <i class="fas fa-chevron-right" style="margin-left:auto;opacity:0.6;"></i>
    </div>
</a>
```

---

### 任务 13：端到端验证

- [ ] **步骤 1：启动应用**

运行：`python app.py`
预期：应用正常启动，无报错

- [ ] **步骤 2：访问 AI 助手页面**

浏览器访问 `/agent/chat`，确认页面正常渲染

- [ ] **步骤 3：测试对话功能**

输入"本月应付账款有多少？"，确认：
1. AI 生成 SQL
2. SQL 通过安全校验
3. 查询执行成功
4. 返回自然语言回答

- [ ] **步骤 4：测试 SQL 安全校验**

确认以下 SQL 被拦截：
- `DELETE FROM Merchant`
- `INSERT INTO Merchant ...`
- `SELECT * FROM Merchant; DROP TABLE Merchant`

- [ ] **步骤 5：测试会话管理**

确认：
1. 新建对话
2. 切换对话
3. 删除对话
4. 对话历史加载
