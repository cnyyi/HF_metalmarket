# 2026-04-24 工作日志

## Admin 首页 500 错误修复
- **现象**：`/admin/` 报 BuildError: Could not build url for endpoint 'utility.utility_list'
- **根因**：`app/routes/utility.py` 的 `pay_reading()` 函数（第534行）语法错误：`return jsonify({'success': False, 'message': ''` 缺右括号，导致 try 块无 except/finally → utility 蓝图注册失败 → 所有依赖 utility 端点的模板渲染崩溃
- **修复**：补全 jsonify 括号、补全 pay_reading 业务逻辑（调用 utility_service.pay_reading 并传 created_by=current_user.id）、补全 except 块

## 微信门户 WxUser/MerchantBinding 表缺失
- **现象**：`/wx/debug/login` 报 `Invalid object name 'WxUser'`
- **根因**：微信门户模块引用了 `WxUser` 和 `MerchantBinding` 两张表，但数据库中从未创建过
- **修复**：创建 WxUser 表（WxUserID/OpenID/UnionID/UserID/Nickname/HeadImgUrl/PhoneNumber/CurrentMerchantID/CreateTime/UpdateTime）+ MerchantBinding 表（BindingID/UserID/MerchantID/BindRole/Status/ApplyTime/ApproveTime/ApproverID/RejectReason/Remark/IsActive）
- **迁移脚本**：`scripts/add_wx_tables.sql`
- **注意**：上次记录说已创建但实际未执行到数据库，本次（21:59）重新执行确认建表成功
