# 2026-04-18 工作日志

## 应收详情弹窗关联数据显示 - Bug修复
- **根因1**：`_get_contract_summary()` SQL引用了不存在的字段（PlotID/MonthlyRent/DepositAmount/SignDate），Contract表实际字段为ContractAmount/AmountReduction/ActualAmount，地块通过ContractPlot中间表关联
- **根因2**：`_get_utility_readings()` 只有两层查询（ReceivableDetail和ReferenceID），缺少基于MerchantID+BelongMonth+MeterType的第三层兜底
- **修复**：重写两个函数，前端适配新字段名

## 抄表应收数据缺失 - Bug修复
- **现象**：11条UtilityReading但只有2条电费应收（MerchantID=11, 13）
- **根因1**：`_create_merged_receivables()` 异常被catch吞掉未re-raise，导致抄表已入库但应收未生成时事务仍commit
- **根因2**：`_create_merged_receivables()` 使用旧ExpenseType表ID（如3=电费），应改为字典表DictID（1017=电费）
- **修复**：异常re-raise + 优先查字典表 + 补生4个商户电费应收 + 修正已有2条ExpenseTypeID 3→1017 + 补建ReceivableDetail
