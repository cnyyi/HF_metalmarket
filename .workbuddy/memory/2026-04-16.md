# 2026-04-16 工作日志

## 修复项
1. **email-validator 依赖缺失**：注册用户报错 `Install 'email_validator'`，已 pip install 并加入 requirements.txt
2. **salary.payslip 路由不存在**：admin_base.html 用户菜单引用不存在的 salary 蓝图路由，导致 BuildError，已移除
3. **salary.profile/monthly 路由不存在**：admin_base.html 导航栏引用 salary 蓝图，已移除
4. **LoginForm 密码长度限制**：LoginForm min=8 导致短密码用户无法登录，移除登录表单的密码长度限制
5. **登录模板缺少字段错误显示**：登录模板只显示 flash 不显示 form.errors，已添加字段错误提示
6. **merchant/list.html 缺 endblock**：`{% block extra_js %}` 缺 `{% endblock %}`
7. **全局纯图标按钮修复**：所有 list 页面的 `btn-outline-*` 纯 `<i>` 图标按钮显示为小圆点，全部加上文字标签（共16处，涉及12个文件）

## 新功能
8. **Admin 首页地块数据统计**
   - 后端：dashboard_stats API 新增地块统计数据（总数/已租/未租 + 按类型面积明细）
   - 前端：地块总览卡片（SVG弧形仪表盘 + 三大数字）+ 地块类型面积卡片（水泥地皮/钢结构厂房/砖混厂房）
   - 样式：工业金属美学主题，渐变图标、进度条动画、fadeSlideUp入场动画
