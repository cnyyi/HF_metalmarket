# 2026-04-17 工作日志

## 暗色模式列表页全面修复

### 问题
- 暗色模式下，大量列表页仍显示浅色效果：Bootstrap 表格（table-striped/table-hover/table-light/table-info）、bg-light 卡片、readonly 输入框、硬编码背景色等

### CSS 修复（admin.css 新增暗色覆盖）
- **表格**：`.table` 的 `--bs-table-*` 变量覆盖（color/bg/striped/hover/border）
- **表头**：`thead > th` 暗色背景
- **table-light**：覆盖为暗色表头
- **table-info / table-active**：暗色覆盖
- **table-bordered**：边框色覆盖
- **bg-light / text-dark**：全局 `!important` 覆盖
- **badge bg-light**：改为半透明主色
- **card bg-light**：覆盖为暗色卡片
- **Modal**：content/header/footer/body/btn-close 全覆盖
- **分页**：page-link 各状态覆盖
- **Nav Tabs**：nav-link / active 状态覆盖
- **Toast / 进度条 / Accordion**：暗色覆盖
- **List Group / hr / pre/code**：暗色覆盖
- **readonly 输入框**：单独覆盖（`var(--color-bg-dark)`）

### 模板修复（硬编码颜色 → CSS 变量）
- **utility/reading_data.html**：`#fff3cd` → `highlight-total` 类，`#e8f4f8` → `highlight-subtotal` 类，`#f8f9fa` → `var(--color-bg-light)`，`#dee2e6` → `var(--color-border)`（含 thead/tfoot/JS 动态渲染，共8处）
- **utility/electricity_meter.html**：group-odd/even 背景色 `#ffffff/#f8f9fa/#e9ecef` → `var(--color-bg-card)/var(--color-bg-light)/var(--table-hover-bg)`
- **expense/detail.html**：3个汇总卡 `#eff6ff/#fef3c7/#fee2e2` → `var(--color-primary-light)/var(--color-warning-light)/var(--color-danger-light)`
- **dorm/occupancy.html**：`badge bg-light text-dark` → `badge bg-secondary`
- **dorm/bill.html**：同上（3处）
- **5个模板的 readonly 输入框**：去掉 `style="background-color: #e9ecef;"`（15处），由 CSS 变量统一控制
  - user/list.html (1), contract/edit.html (5), contract/add.html (4), plot/edit.html (3), plot/add.html (2)

### 未修改（合理保留）
- auth_base.html / public_base.html / merchant_base.html 的公共页背景色（独立主题体系）
- admin/index.html 的数据色点（饼图图例、装饰色）
- scale/list.html 的 modal-header 渐变（暗色下效果可接受）

## 架构重构：contract.py / plot.py 去除 routes 层直写 SQL

### 变更
- **新建** `app/services/contract_service.py`（423行）— 从 contract.py 提取9个方法到 ContractService
  - get_contract_periods / get_available_merchants / get_available_plots / generate_contract_number
  - get_contract_list / add_contract / get_contract_detail / update_contract / delete_contract
- **新建** `app/services/plot_service.py`（387行）— 从 plot.py 提取10个方法到 PlotService
  - get_plot_types / get_plot_types_json / save_uploaded_file / safe_delete_image / calculate_rent
  - add_plot / update_plot / upload_image / delete_plot / get_plot_detail / get_plot_list
- **重构** `app/routes/contract.py` — 删除所有 DBConnection 直接调用，改为 ContractService 调用（从~380行缩减到225行）
- **重构** `app/routes/plot.py` — 删除所有 DBConnection 直接调用，改为 PlotService 调用（从~350行缩减到239行）
- 两个 routes 文件现在都是薄路由层，只做参数校验+调用 Service+返回响应

### 过磅统计过滤逻辑修正
- admin.py dashboard_stats：当日过磅统计 `COALESCE(TareTime, GrossTime, ScaleTime)` → `ScaleTime`
- scale_service.py：进出厂统计/月度走势/今日记录 同步改为 ScaleTime 过滤

### app.py 环境变量加载
- 新增 `from dotenv import load_dotenv` + `load_dotenv()`，确保开发环境自动读取 .env 文件

### UI 微调
- merchant/list.html + user/list.html：按钮添加文字标签（编辑/删除/门户管理）
- contract/list.html：操作列宽度 100px→130px，添加 title 属性
- 7个模板的 readonly 输入框去掉硬编码 `style="background-color: #e9ecef;"`（由 CSS 变量统一控制）

### 技术债务清理状态
- ~~架构违规：contract.py、plot.py 在routes层直接使用DBConnection~~ → ✅ 已修复
- 架构违规：admin.py 仍直接使用 DBConnection（仪表盘统计查询，属于合理场景，暂保留）
- Dead code：app/extensions.py 未被引用
- 调试代码：utility.py 仍有部分 print() 语句
- 财务导出：应收/应付/现金流水导出Excel功能均未实现
