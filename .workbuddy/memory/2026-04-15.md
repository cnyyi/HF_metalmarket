# 2026-04-15 工作日志

## 工资管理模块开发（公司内部）

### 需求确认
- 杨苡提出新增工资管理模块，供公司及市场商户使用
- 建议分两步走：先做公司内部，商户远期再议
- 杨苡确认先做公司内部方案

### 完成内容
1. **数据库设计**：SalaryProfile + SalaryRecord 两张表，迁移脚本 `scripts/add_salary_tables.sql`
   - 含字典数据（salary_status/salary_profile_status/工资费用项）、权限（salary_manage）
2. **服务层**：`app/services/salary_service.py` — SalaryService
   - 档案CRUD、月度批量生成、编辑工资单、审核/批量审核、发放（联动财务4步）、工资条查看、汇总统计
3. **路由层**：`app/routes/salary.py` — salary_bp
4. **页面模板**：3个页面
   - salary/profile.html — 工资档案管理
   - salary/monthly.html — 月度工资核算（含汇总统计+批量操作）
   - salary/payslip.html — 我的工资条（员工自助）
5. **系统集成**：
   - 蓝图注册到 app/__init__.py
   - 导航菜单：财务管理下拉新增"工资档案"+"月度工资"，用户菜单新增"我的工资条"
   - 权限 salary_manage 已赋权 admin + staff 角色

### 数据库迁移执行
- 使用 sqlcmd 逐段执行迁移脚本（GO 分隔符在 sqlcmd -i 模式下有问题，改用 -Q 逐段执行）
- 验证结果：2张表 + 6条字典 + 1条权限 + 2条角色赋权，全部到位

### 宿舍管理模块设计
- 杨苡提出新增宿舍管理模块
- 需求确认：自建宿舍楼 + 扁平房间编号 + 商户/个人双租户类型
- 收费模式：租金固定 + 水费定额 + 电费抄表
- 不签合同，按月出账单，联动应收
- 需要身份证号和照片上传
- 选定方案二：四表分离（Room + Occupancy + Reading + Bill）
- 设计文档：`docs/plans/2026-04-15-dormitory-design.md`
- 实施计划：`docs/plans/2026-04-15-dormitory-plan.md`

### 宿舍管理模块开发完成
1. **数据库**：4张新表 DormRoom/DormOccupancy/DormReading/DormBill，迁移脚本 `scripts/add_dorm_tables.sql`
   - 字典5组(dorm_room_type/status/tenant_type/occupancy_status/bill_status)
   - 费用项3个(宿舍租金/水费/电费) + 客户类型1个(宿舍个人)
   - 权限 dorm_manage 已赋权 admin + staff
2. **服务层**：`app/services/dorm_service.py` — DormService
   - 房间CRUD、入住/退房（联动房间状态）、电表读数（自动计算）、账单生成/确认/开账
   - 财务联动：开账→创建3条Receivable（租金/水费/电费），个人租户自动在Customer表查找/创建
3. **路由层**：`app/routes/dorm.py` — dorm_bp，20个接口
4. **页面模板**：4个页面
   - dorm/rooms.html — 房间管理（CRUD+快速入住）
   - dorm/occupancy.html — 入住管理（入住/退房+身份证上传+照片查看）
   - dorm/reading.html — 电表抄表（批量录入+历史查询）
   - dorm/bill.html — 月度账单（生成/确认/开账+批量操作+汇总卡片）
5. **系统集成**：
   - 蓝图注册到 app/__init__.py
   - 导航菜单：市场管理下拉新增分割线+4项宿舍菜单
   - 上传目录：uploads/dorm_idcard/
6. **踩坑**：DormService中Python代码误用SQL Server的N'...'前缀语法，已修复

## 收款核销外键修复
- **问题**：应收账款页面点击收款按钮报错
- **根因**：`CollectionRecord` 表的外键 `FK__Collectio__Recei__160F4887` 指向的是 `Receivable_Backup_20260404`（备份表），而非 `Receivable`（当前表）
- **修复**：删除错误外键，重建 `FK_CollectionRecord_Receivable` 指向 `Receivable.ReceivableID`
- **注意**：这个外键问题应该是在某次表备份/恢复时产生的，新建的应收记录ID在备份表中不存在导致 INSERT 失败

## 财务管理增强 P1：账户体系 + 直接记账

### 需求分析（brainstorming）
- 杨苡确认6大痛点：直接记账/多账户/预收预付/保证金/内部调拨/计次计量业务
- 待确认项已明确：垃圾清运供应商不固定/过磅保持独立/一条预收冲多条应收/押金转抵限同商户

### P1 完成内容
1. **Account 表**：AccountID/AccountName/AccountType(Cash/Bank/WeChat)/BankName/BankAccount/Balance/IsDefault/Status/Remark
   - 初始化4个默认账户：现金/银行(对公)/银行(对私)/微信
2. **CashFlow 扩展**：+AccountID(关联Account) +TransactionNo(流水号CF20260415001)
3. **AccountService**：账户CRUD + 余额操作(_adjust_balance) + 汇总统计
4. **FinanceService 扩展**：
   - collect_receivable/pay_payable 新增 account_id 参数 → 自动调整账户余额
   - direct_entry 直接记账（无需应收/应付前置）→ CashFlow + Account余额联动
   - _generate_transaction_no 自动流水号
5. **路由**：finance_bp 新增 account(6个路由) + direct_entry(2个路由) + account_active_list(公共)
6. **权限**：account_manage + direct_entry，已赋权 admin + staff
7. **模板**：account.html(账户管理) + direct_entry.html(直接记账) + cash_flow.html(加账户列+筛选)
8. **改造**：receivable.html/payable.html 收付款弹窗加入账户选择下拉
9. **导航**：财务管理下拉新增"账户管理"和"直接记账"
10. **迁移脚本**：scripts/add_account_tables.sql

## 财务管理增强 P2：预收预付 + 押金管理

### P2 完成内容（代码已写，待执行迁移脚本）

1. **4张新表**：
   - Prepayment：预收/预付主表（Direction/CustomerType/CustomerID/TotalAmount/AppliedAmount/RemainingAmount/Status/AccountID/CashFlowID）
   - PrepaymentApply：冲抵明细（PrepaymentID/ReceivableID/PayableID/Amount）— 支持一条预收冲多条应收
   - Deposit：押金主表（CustomerType/CustomerID/DepositType/Amount/RefundAmount/DeductAmount/TransferAmount/Status/AccountID）
   - DepositOperation：操作记录（DepositID/OperationType/Amount/AccountID/ReceivableID/CashFlowID）
2. **PrepaymentService**：CRUD + 创建(4步联动) + 冲抵核销(支持1对多) + 获取可用预收 + 汇总统计
3. **DepositService**：CRUD + 收取(4步联动) + 退还(4步联动) + 扣除(无资金流出) + 转抵(限同商户应收) + 操作记录 + 汇总
4. **路由**：finance_bp 新增 14 个路由（预收预付7 + 押金7）
5. **权限**：prepayment_manage + deposit_manage，迁移脚本中已赋权 admin + staff
6. **模板**：prepayment.html(预收预付管理) + deposit.html(押金管理)
7. **改造**：receivable.html 收款弹窗增加"预收冲抵"模式切换
8. **导航**：财务管理下拉新增"预收预付"和"押金管理"
9. **字典**：prepayment_status(3)/deposit_type(3)/deposit_status(3)/prepayment_direction(2)
10. **迁移脚本**：scripts/add_p2_tables.sql

### P2 迁移脚本执行（23:13）

- **问题**：sqlcmd -i 执行脚本因 emoji 字符导致静默失败（exitCode=0 但表未创建）
- **解决**：用 -Q 参数逐段执行，去除 emoji，4张表+11条字典+2个权限+4条角色赋权全部成功
- **注意**：今后迁移脚本避免使用 emoji/特殊字符，或用 -Q 逐段执行
- **验证**：4表 + 11字典 + 2权限 + 4角色赋权 全部到位

## 首页仪表盘地块数据修复（23:30）

### 问题
- 地块总览全部数字为0，地块面积明细一直显示加载中
- **根因1**：仪表盘SQL查 `Status = N'已租'`，但实际状态值是 `N'已出租'`
- **根因2**：11条Plot记录的Status字段为乱码 `??`（0x3F003F00），是早期pyodbc写入中文编码问题

### 修复
1. 修复数据：`UPDATE Plot SET Status = N'已出租' WHERE UNICODE(Status) = 63` — 影响11行
2. 修复代码：admin.py dashboard_stats 中3处 `N'已租'` → `N'已出租'`
3. 修复后验证：18总/11已出租/7未出租，面积明细三种类型均有数据
